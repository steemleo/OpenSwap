## What & why

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `npm run typecheck` and `npm test` pass
- [ ] Money paths keep exact BigInt/decimal-string math (no floats)
- [ ] No secrets in argv, logs, errors, or receipts
- [ ] Machine output (`--json`) stays additive — no removed/renamed fields
- [ ] Docs updated if flags or behavior changed
